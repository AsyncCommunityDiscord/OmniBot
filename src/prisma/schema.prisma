
// === Modèles de core\models\modules.prisma ===
model ModuleActivation {
  moduleId String
  guildId  String

  activated        Boolean
  activatedVersion String  @default("0.0.0")

  @@id([moduleId, guildId])
}
